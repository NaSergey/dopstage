import localFont from "next/font/local";

export const chivoMono = localFont({
  src: "../fonts/ChivoMono[wght].woff",
  variable: "--font-chivo-mono",
  weight: "100 900",
  display: "swap",
});

export const chivo = localFont({
  src: "../fonts/Chivo.woff",
  variable: "--font-chivo",
  display: "swap",
});

